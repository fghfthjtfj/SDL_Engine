#version 450
layout(local_size_x = 64) in;

layout(set=1, binding=0) buffer CountBuf { uint counts[]; };

void main() {
    uint id = gl_GlobalInvocationID.x;
    if (id >= uint(counts.length())) return;
    counts[id] = 0;
}